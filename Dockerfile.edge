FROM ros:jazzy-ros-base

RUN apt-get update && apt-get install -y --no-install-recommends \
    python3-pip \
    python3-colcon-common-extensions \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir --break-system-packages \
    uv

WORKDIR /aether

COPY pyproject.toml pyproject.toml
COPY uv.lock uv.lock
COPY config.py config.py
COPY src/edge_node src/edge_node

ENV PYTHONPATH=/aether

RUN UV_CACHE_DIR=/tmp/uv-cache uv export --group edge --frozen --no-hashes --format requirements-txt -o /tmp/requirements-edge.txt && \
    UV_CACHE_DIR=/tmp/uv-cache uv pip install --system -r /tmp/requirements-edge.txt

RUN . /opt/ros/jazzy/setup.sh && \
    colcon build --packages-select edge_node

RUN echo "source /opt/ros/jazzy/setup.bash" >> /root/.bashrc
RUN echo "source /aether/install/setup.bash" >> /root/.bashrc

CMD ["/bin/bash", "-c", "source /opt/ros/jazzy/setup.bash && source /aether/install/setup.bash && ros2 run edge_node edge_node"]
