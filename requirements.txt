# Python dependencies used by the repo's Python ROS 2 nodes.
#
# Install these inside your virtual environment:
#   pip install -r requirements.txt
#
# This repo also depends on a ROS 2 Jazzy installation that provides:
#   - rclpy
#   - std_msgs
#   - sensor_msgs
#   - geometry_msgs
#   - vision_msgs
#
# On Ubuntu with ROS 2 Jazzy, that usually means installing/sourcing ROS and,
# for the vision node, ensuring the message package exists:
#   sudo apt install ros-jazzy-vision-msgs
#
# OpenCV may also need system libraries such as:
#   sudo apt install libgl1

adafruit-blinka
adafruit-circuitpython-dht
lgpio
opencv-python-headless
pydantic
pydantic-settings
pymavlink
ultralytics
